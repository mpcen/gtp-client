export enum OverlayTypes {
    None,
    LeagueSelect,
    TeamSelect,
    GTRInfo,
    YearSelect,
    RemoveLeague,
}

export enum TeamDestination {
    Away = 'away',
    Home = 'home',
}

export enum MatchWinner {
    Away = 'away',
    Home = 'home',
}
